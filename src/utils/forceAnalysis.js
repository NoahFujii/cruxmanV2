/**
 * Force analysis engine — stub placeholder.
 * Replace with the real implementation when the physics engine is built.
 *
 * runFullAnalysis(frame, climberStats) is called by useClimbStore.analyzeFrame.
 * Return null here so analysisResult stays null until the engine exists.
 */
export function runFullAnalysis(_frame, _climberStats) {
  return null;
}
